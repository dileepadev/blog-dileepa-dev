---
// Welcome component for the blog home page
---

<div
  class="min-h-screen bg-linear-to-br from-blue-50 via-white to-purple-50 flex items-center justify-center px-4"
>
  <div class="max-w-4xl mx-auto text-center">
    <!-- Main Heading -->
    <h1
      class="text-5xl md:text-7xl font-bold bg-linear-to-r from-blue-600 via-purple-600 to-indigo-600 bg-clip-text text-transparent mb-6 animate-fade-in"
    >
      Welcome to Dileepa's Dev Blog
    </h1>

    <!-- Subtitle -->
    <p
      class="text-xl md:text-2xl text-gray-600 mb-8 max-w-2xl mx-auto leading-relaxed"
    >
      Exploring the frontiers of <span class="font-semibold text-blue-600"
        >Artificial Intelligence</span
      >,
      <span class="font-semibold text-purple-600">Cloud Computing</span>, and
      <span class="font-semibold text-indigo-600">Software Development</span>
    </p>

    <!-- Description -->
    <p class="text-lg text-gray-500 mb-12 max-w-3xl mx-auto">
      Dive into insightful tutorials, cutting-edge projects, and
      thought-provoking discussions about the technologies shaping our future.
    </p>

    <!-- CTA Buttons -->
    <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
      <a
        href="/blog"
        class="px-8 py-4 bg-linear-to-r from-blue-600 to-purple-600 text-white font-semibold rounded-lg shadow-lg hover:shadow-xl transform hover:scale-105 transition-all duration-300"
      >
        Explore Articles
      </a>
      <a
        href="/about"
        class="px-8 py-4 border-2 border-gray-300 text-gray-700 font-semibold rounded-lg hover:border-blue-500 hover:text-blue-600 transition-all duration-300"
      >
        Learn More
      </a>
    </div>

    <!-- Decorative Elements -->
    <div class="mt-16 flex justify-center space-x-8 opacity-60">
      <div
        class="w-16 h-16 bg-blue-100 rounded-full flex items-center justify-center animate-bounce"
      >
        <span class="text-2xl">🤖</span>
      </div>
      <div
        class="w-16 h-16 bg-purple-100 rounded-full flex items-center justify-center animate-bounce"
        style="animation-delay: 0.2s"
      >
        <span class="text-2xl">☁️</span>
      </div>
      <div
        class="w-16 h-16 bg-indigo-100 rounded-full flex items-center justify-center animate-bounce"
        style="animation-delay: 0.4s"
      >
        <span class="text-2xl">💻</span>
      </div>
    </div>
  </div>
</div>

<style>
  @keyframes fade-in {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in {
    animation: fade-in 1s ease-out;
  }
</style>
